# 2026-06-01

## Pre-compaction memory flush — RWI / Watch Desk price extraction
- Ongoing task: Fix RWI hourly extraction and Watch Desk v2 pricing so active/pending/for-sale RWI posts show real asking prices instead of `Price n/a` or bad fallback values like `$289`.
- Stable fixes already made earlier in this session:
  - `rwi_mvp/run_bookmarklet_extractor.mjs` and `watch_desk/scripts/extract_rwi_images.mjs` were hardened to use `open` + `focus` instead of brittle `navigate`/`wait` to avoid `chrome://new-tab-page/` / destroyed context races.
  - `rwi_mvp/src/run-crawl.js` was patched to flatten nested bookmarklet rows (`row: {...}`) and drop malformed `undefined`/`Untitled` rows.
  - `watch_desk/scripts/build_watch_desk.py` skips malformed RWI entries.
  - `watch_desk/scripts/build_v2_html.py` was restored to normal filtering; do not force sold RWI listings to always show.
  - `rwi_mvp/delta_compare.mjs` recovery gate was adjusted so same-day recovered rows can pass the suspicious-baseline safety check.
- Current bug state:
  - Bobby reported: “Now all the prices are coming back price n/a” and then “FIX IT!”
  - Logs showed hourly job output still producing bad price data, e.g. latest RWI listing with `askingPrice: "$289"`, `priceValue: 289` even when the live thread’s real Asking Price field differs.
  - The crawler path is `rwi_mvp/run_hourly_cron.sh` → `rwi_mvp/run_fresh_delta.mjs` → `rwi_mvp/src/run-crawl.js` → `rwi_mvp/run_bookmarklet_extractor.mjs`.
  - Important discovery: the hourly job ultimately goes through `src/run-crawl.js`; fixing only the wrapped bookmarklet/extractor display logic was insufficient unless the data written by `run-crawl.js` is corrected.
  - Another discovery: generic `getField(..., "Price")` fallback in extractor was likely matching unrelated page price-ish data and causing `$289`; it was removed/should stay removed. Prefer exact `Asking Price?` / `Asking Price` only.
- Attempted live-browser price recovery:
  - Tried to overwrite bad prices by opening each thread with `openclaw browser`, focusing the new tab, and evaluating `document.body.innerText.match(/Asking Price\?\s*([£$€]\s*[0-9][0-9,]*(?:\.[0-9]{1,2})?)/i)?.[1]`.
  - Found that the exact live thread DOM can expose the real Asking Price, but wiring it into the hourly/background run caused environment/profile contention: background hourly job cannot safely own the same `openclaw` browser profile as the live assistant session. That crashed the run.
  - There was also a regression where `pricedRows = extractedRows.map(enrichLivePrice)` was inserted before `enrichLivePrice` was declared in `rwi_mvp/src/run-crawl.js`; this init-order bug was noticed and needs to stay fixed if any such code remains.
- Immediate next safest steps:
  1. Inspect current `rwi_mvp/src/run-crawl.js` and remove or disable the browser-profile live price recovery code if it is still present and destabilizing the hourly job.
  2. Restore hourly stability first: rerun `rwi_mvp/run_hourly_cron.sh` and ensure it completes without browser profile contention.
  3. Then fix price extraction in `run_bookmarklet_extractor.mjs` itself using the exact rendered/listing-session context, but avoid per-row CLI browser opens from `src/run-crawl.js` unless using a separate safe browser profile or proven non-conflicting method.
  4. Verify real output in:
     - `rwi_mvp/data/rwi_bookmarklet_current.json`
     - `rwi_mvp/data/rwi.json`
     - `watch_desk/public/data/latest.json`
     - `watch_desk/public/v2.html`
- User preference/constraint: fix it directly, do not just report status; don’t break prior stability fixes; do not force sold RWI listings to always appear.
# 2026-06-01

## Pre-compaction memory flush — RWI / Watch Desk price extraction
- Ongoing task: Fix RWI hourly extraction and Watch Desk v2 pricing so active/pending/for-sale RWI posts show real asking prices instead of `Price n/a` or bad fallback values like `$289`.
- Stable fixes already made earlier in this session:
  - `rwi_mvp/run_bookmarklet_extractor.mjs` and `watch_desk/scripts/extract_rwi_images.mjs` were hardened to use `open` + `focus` instead of brittle `navigate`/`wait` to avoid `chrome://new-tab-page/` / destroyed context races.
  - `rwi_mvp/src/run-crawl.js` was patched to flatten nested bookmarklet rows (`row: {...}`) and drop malformed `undefined`/`Untitled` rows.
  - `watch_desk/scripts/build_watch_desk.py` skips malformed RWI entries.
  - `watch_desk/scripts/build_v2_html.py` was restored to normal filtering; do not force sold RWI listings to always show.
  - `rwi_mvp/delta_compare.mjs` recovery gate was adjusted so same-day recovered rows can pass the suspicious-baseline safety check.
- Current bug state:
  - Bobby reported: “Now all the prices are coming back price n/a” and then “FIX IT!”
  - Logs showed hourly job output still producing bad price data, e.g. latest RWI listing with `askingPrice: "$289"`, `priceValue: 289` even when the live thread’s real Asking Price field differs.
  - The crawler path is `rwi_mvp/run_hourly_cron.sh` → `rwi_mvp/run_fresh_delta.mjs` → `rwi_mvp/src/run-crawl.js` → `rwi_mvp/run_bookmarklet_extractor.mjs`.
  - Important discovery: the hourly job ultimately goes through `src/run-crawl.js`; fixing only the wrapped bookmarklet/extractor display logic was insufficient unless the data written by `run-crawl.js` is corrected.
  - Another discovery: generic `getField(..., "Price")` fallback in extractor was likely matching unrelated page price-ish data and causing `$289`; it was removed/should stay removed. Prefer exact `Asking Price?` / `Asking Price` only.
- Attempted live-browser price recovery:
  - Tried to overwrite bad prices by opening each thread with `openclaw browser`, focusing the new tab, and evaluating `document.body.innerText.match(/Asking Price\?\s*([£$€]\s*[0-9][0-9,]*(?:\.[0-9]{1,2})?)/i)?.[1]`.
  - Found that the exact live thread DOM can expose the real Asking Price, but wiring it into the hourly/background run caused environment/profile contention: background hourly job cannot safely own the same `openclaw` browser profile as the live assistant session. That crashed the run.
  - There was also a regression where `pricedRows = extractedRows.map(enrichLivePrice)` was inserted before `enrichLivePrice` was declared in `rwi_mvp/src/run-crawl.js`; this init-order bug was noticed and needs to stay fixed if any such code remains.
- Immediate next safest steps:
  1. Inspect current `rwi_mvp/src/run-crawl.js` and remove or disable the browser-profile live price recovery code if it is still present and destabilizing the hourly job.
  2. Restore hourly stability first: rerun `rwi_mvp/run_hourly_cron.sh` and ensure it completes without browser profile contention.
  3. Then fix price extraction in `run_bookmarklet_extractor.mjs` itself using the exact rendered/listing-session context, but avoid per-row CLI browser opens from `src/run-crawl.js` unless using a separate safe browser profile or proven non-conflicting method.
  4. Verify real output in:
     - `rwi_mvp/data/rwi_bookmarklet_current.json`
     - `rwi_mvp/data/rwi.json`
     - `watch_desk/public/data/latest.json`
     - `watch_desk/public/v2.html`
- User preference/constraint: fix it directly, do not just report status; don’t break prior stability fixes; do not force sold RWI listings to always appear.

## Pre-compaction continuation — current working state after angry correction
- Bobby is understandably upset: prior work overcomplicated a working RWI/Watch Desk pipeline, made hourly unstable, and caused wrong per-listing prices. Tone requirement: stop sending noisy Telegram status updates; work quietly and return only with a concrete workable fix/verification.
- Current modified file explicitly observed: `/Users/bobby/.openclaw/workspace/rwi_mvp/run_bookmarklet_extractor.mjs`.
- Critical remaining instability in `run_bookmarklet_extractor.mjs`: helper functions `parseLivePriceText` and `extractLivePriceViaBrowser` plus the post-processing loop that calls `extractLivePriceViaBrowser(row.url)` for each parsed row. This reintroduces per-row `openclaw browser open/focus/evaluate/close` during the hourly job and is the known source/risk of `Port 18800 is in use for profile "openclaw" but not by openclaw`. Remove/disable this live browser CLI enrichment path before rerunning hourly.
- In the same file, the in-page enrichment string still has a broad final fallback: `askingPrice = parsePrice(plainText, { requireCurrency: true }) || parsePrice(plainText);`. This can grab unrelated page/listing text such as `$289`. It should be removed or restricted to exact Asking Price field/text/custom-field matches only.
- Reliable price source discovered: thread page custom fields are readable when fetched/opened in the RWI session. Example thread `https://forum.replica-watch.info/threads/tudor-pelagos-39-v2-w-bracelet-gen-rubber.11043894/` exposes `Asking Price? = £450`, `Currency Accepted? = GBP`, `Shipping Costs? = £10 UK...`. Correct approach: parse `dl.pairs--customField` / exact `Asking Price?` field or exact `Asking Price? £...` line; do not parse whole body/page as price.
- Preserve prior good changes: keep `open` + `focus` stabilization; keep malformed row filtering; keep v2 normal filter (don’t force sold listings); keep same-day delta recovery gate; keep removal of generic `getField(..., "Price")`.
- Next implementation recipe after compaction: edit `run_bookmarklet_extractor.mjs` to (1) remove `parseLivePriceText`, `extractLivePriceViaBrowser`, and the per-row livePrice override loop, and (2) remove the final whole-plainText price fallback. Then run `bash /Users/bobby/.openclaw/workspace/rwi_mvp/run_hourly_cron.sh`. Verify no profile contention/init-order errors and inspect JSON outputs for correct non-`$289` prices.
# 2026-06-01

## Pre-compaction memory flush — RWI / Watch Desk price extraction
- Ongoing task: Fix RWI hourly extraction and Watch Desk v2 pricing so active/pending/for-sale RWI posts show real asking prices instead of `Price n/a` or bad fallback values like `$289`.
- Stable fixes already made earlier in this session:
  - `rwi_mvp/run_bookmarklet_extractor.mjs` and `watch_desk/scripts/extract_rwi_images.mjs` were hardened to use `open` + `focus` instead of brittle `navigate`/`wait` to avoid `chrome://new-tab-page/` / destroyed context races.
  - `rwi_mvp/src/run-crawl.js` was patched to flatten nested bookmarklet rows (`row: {...}`) and drop malformed `undefined`/`Untitled` rows.
  - `watch_desk/scripts/build_watch_desk.py` skips malformed RWI entries.
  - `watch_desk/scripts/build_v2_html.py` was restored to normal filtering; do not force sold RWI listings to always show.
  - `rwi_mvp/delta_compare.mjs` recovery gate was adjusted so same-day recovered rows can pass the suspicious-baseline safety check.
- Current bug state:
  - Bobby reported: “Now all the prices are coming back price n/a” and then “FIX IT!”
  - Logs showed hourly job output still producing bad price data, e.g. latest RWI listing with `askingPrice: "$289"`, `priceValue: 289` even when the live thread’s real Asking Price field differs.
  - The crawler path is `rwi_mvp/run_hourly_cron.sh` → `rwi_mvp/run_fresh_delta.mjs` → `rwi_mvp/src/run-crawl.js` → `rwi_mvp/run_bookmarklet_extractor.mjs`.
  - Important discovery: the hourly job ultimately goes through `src/run-crawl.js`; fixing only the wrapped bookmarklet/extractor display logic was insufficient unless the data written by `run-crawl.js` is corrected.
  - Another discovery: generic `getField(..., "Price")` fallback in extractor was likely matching unrelated page price-ish data and causing `$289`; it was removed/should stay removed. Prefer exact `Asking Price?` / `Asking Price` only.
- Attempted live-browser price recovery:
  - Tried to overwrite bad prices by opening each thread with `openclaw browser`, focusing the new tab, and evaluating `document.body.innerText.match(/Asking Price\?\s*([£$€]\s*[0-9][0-9,]*(?:\.[0-9]{1,2})?)/i)?.[1]`.
  - Found that the exact live thread DOM can expose the real Asking Price, but wiring it into the hourly/background run caused environment/profile contention: background hourly job cannot safely own the same `openclaw` browser profile as the live assistant session. That crashed the run.
  - There was also a regression where `pricedRows = extractedRows.map(enrichLivePrice)` was inserted before `enrichLivePrice` was declared in `rwi_mvp/src/run-crawl.js`; this init-order bug was noticed and needs to stay fixed if any such code remains.
- Immediate next safest steps:
  1. Inspect current `rwi_mvp/src/run-crawl.js` and remove or disable the browser-profile live price recovery code if it is still present and destabilizing the hourly job.
  2. Restore hourly stability first: rerun `rwi_mvp/run_hourly_cron.sh` and ensure it completes without browser profile contention.
  3. Then fix price extraction in `run_bookmarklet_extractor.mjs` itself using the exact rendered/listing-session context, but avoid per-row CLI browser opens from `src/run-crawl.js` unless using a separate safe browser profile or proven non-conflicting method.
  4. Verify real output in:
     - `rwi_mvp/data/rwi_bookmarklet_current.json`
     - `rwi_mvp/data/rwi.json`
     - `watch_desk/public/data/latest.json`
     - `watch_desk/public/v2.html`
- User preference/constraint: fix it directly, do not just report status; don’t break prior stability fixes; do not force sold RWI listings to always appear.
# 2026-06-01

## Pre-compaction memory flush — RWI / Watch Desk price extraction
- Ongoing task: Fix RWI hourly extraction and Watch Desk v2 pricing so active/pending/for-sale RWI posts show real asking prices instead of `Price n/a` or bad fallback values like `$289`.
- Stable fixes already made earlier in this session:
  - `rwi_mvp/run_bookmarklet_extractor.mjs` and `watch_desk/scripts/extract_rwi_images.mjs` were hardened to use `open` + `focus` instead of brittle `navigate`/`wait` to avoid `chrome://new-tab-page/` / destroyed context races.
  - `rwi_mvp/src/run-crawl.js` was patched to flatten nested bookmarklet rows (`row: {...}`) and drop malformed `undefined`/`Untitled` rows.
  - `watch_desk/scripts/build_watch_desk.py` skips malformed RWI entries.
  - `watch_desk/scripts/build_v2_html.py` was restored to normal filtering; do not force sold RWI listings to always show.
  - `rwi_mvp/delta_compare.mjs` recovery gate was adjusted so same-day recovered rows can pass the suspicious-baseline safety check.
- Current bug state:
  - Bobby reported: “Now all the prices are coming back price n/a” and then “FIX IT!”
  - Logs showed hourly job output still producing bad price data, e.g. latest RWI listing with `askingPrice: "$289"`, `priceValue: 289` even when the live thread’s real Asking Price field differs.
  - The crawler path is `rwi_mvp/run_hourly_cron.sh` → `rwi_mvp/run_fresh_delta.mjs` → `rwi_mvp/src/run-crawl.js` → `rwi_mvp/run_bookmarklet_extractor.mjs`.
  - Important discovery: the hourly job ultimately goes through `src/run-crawl.js`; fixing only the wrapped bookmarklet/extractor display logic was insufficient unless the data written by `run-crawl.js` is corrected.
  - Another discovery: generic `getField(..., "Price")` fallback in extractor was likely matching unrelated page price-ish data and causing `$289`; it was removed/should stay removed. Prefer exact `Asking Price?` / `Asking Price` only.
- Attempted live-browser price recovery:
  - Tried to overwrite bad prices by opening each thread with `openclaw browser`, focusing the new tab, and evaluating `document.body.innerText.match(/Asking Price\?\s*([£$€]\s*[0-9][0-9,]*(?:\.[0-9]{1,2})?)/i)?.[1]`.
  - Found that the exact live thread DOM can expose the real Asking Price, but wiring it into the hourly/background run caused environment/profile contention: background hourly job cannot safely own the same `openclaw` browser profile as the live assistant session. That crashed the run.
  - There was also a regression where `pricedRows = extractedRows.map(enrichLivePrice)` was inserted before `enrichLivePrice` was declared in `rwi_mvp/src/run-crawl.js`; this init-order bug was noticed and needs to stay fixed if any such code remains.
- Immediate next safest steps:
  1. Inspect current `rwi_mvp/src/run-crawl.js` and remove or disable the browser-profile live price recovery code if it is still present and destabilizing the hourly job.
  2. Restore hourly stability first: rerun `rwi_mvp/run_hourly_cron.sh` and ensure it completes without browser profile contention.
  3. Then fix price extraction in `run_bookmarklet_extractor.mjs` itself using the exact rendered/listing-session context, but avoid per-row CLI browser opens from `src/run-crawl.js` unless using a separate safe browser profile or proven non-conflicting method.
  4. Verify real output in:
     - `rwi_mvp/data/rwi_bookmarklet_current.json`
     - `rwi_mvp/data/rwi.json`
     - `watch_desk/public/data/latest.json`
     - `watch_desk/public/v2.html`
- User preference/constraint: fix it directly, do not just report status; don’t break prior stability fixes; do not force sold RWI listings to always appear.

## Pre-compaction continuation — current working state after angry correction
- Bobby is understandably upset: prior work overcomplicated a working RWI/Watch Desk pipeline, made hourly unstable, and caused wrong per-listing prices. Tone requirement: stop sending noisy Telegram status updates; work quietly and return only with a concrete workable fix/verification.
- Current modified file explicitly observed: `/Users/bobby/.openclaw/workspace/rwi_mvp/run_bookmarklet_extractor.mjs`.
- Critical remaining instability in `run_bookmarklet_extractor.mjs`: helper functions `parseLivePriceText` and `extractLivePriceViaBrowser` plus the post-processing loop that calls `extractLivePriceViaBrowser(row.url)` for each parsed row. This reintroduces per-row `openclaw browser open/focus/evaluate/close` during the hourly job and is the known source/risk of `Port 18800 is in use for profile "openclaw" but not by openclaw`. Remove/disable this live browser CLI enrichment path before rerunning hourly.
- In the same file, the in-page enrichment string still has a broad final fallback: `askingPrice = parsePrice(plainText, { requireCurrency: true }) || parsePrice(plainText);`. This can grab unrelated page/listing text such as `$289`. It should be removed or restricted to exact Asking Price field/text/custom-field matches only.
- Reliable price source discovered: thread page custom fields are readable when fetched/opened in the RWI session. Example thread `https://forum.replica-watch.info/threads/tudor-pelagos-39-v2-w-bracelet-gen-rubber.11043894/` exposes `Asking Price? = £450`, `Currency Accepted? = GBP`, `Shipping Costs? = £10 UK...`. Correct approach: parse `dl.pairs--customField` / exact `Asking Price?` field or exact `Asking Price? £...` line; do not parse whole body/page as price.
- Preserve prior good changes: keep `open` + `focus` stabilization; keep malformed row filtering; keep v2 normal filter (don’t force sold listings); keep same-day delta recovery gate; keep removal of generic `getField(..., "Price")`.
- Next implementation recipe after compaction: edit `run_bookmarklet_extractor.mjs` to (1) remove `parseLivePriceText`, `extractLivePriceViaBrowser`, and the per-row livePrice override loop, and (2) remove the final whole-plainText price fallback. Then run `bash /Users/bobby/.openclaw/workspace/rwi_mvp/run_hourly_cron.sh`. Verify no profile contention/init-order errors and inspect JSON outputs for correct non-`$289` prices.

## Pre-compaction continuation — RWI stabilized and Watch Desk verified
- RWI/Watch Desk was later restored to a stable path:
  - `rwi_mvp/run_bookmarklet_extractor.mjs` no longer uses per-row `openclaw browser` CLI extraction.
  - Popup/window-opening logic was removed from both `run_bookmarklet_extractor.mjs` and `rwi_mvp/rwi_bookmarklet_extractor_wrapped.js`.
  - Enrichment now uses same-session authenticated `fetch(row.url, { credentials: 'include' })` from the page context and exact custom-field parsing.
  - Chunk row shape fixed to `chunk.map(function(r) { return r.row; })`.
  - GBP/EUR/USD parsing fixed via simple symbol checks plus `EUR|GBP|USD` regex.
- Verification completed:
  - `bash rwi_mvp/run_hourly_cron.sh` completed successfully without browser profile contention.
  - `rwi_mvp/data/rwi.json` count was 333.
  - `watch_desk/public/data/latest.json` had active RWI rows with correct prices.
  - Checks showed `bad289: 0` and `priceNaActive: 0`.
  - Later heartbeat logs indicated the hourly RWI job continued to run and report new posts correctly.

## Nutrition log — 2026-06-01
- Logged breakfast: sausage egg and cheese on bagel = 720 kcal, C 58g, F 39g, P 31g.
- Logged dinner/snacks/drinks and manually corrected estimates:
  - another half sausage egg and cheese sandwich = 360 kcal
  - half chicken bacon ranch quesadilla from Del Fuego = 450 kcal
  - half cup guacamole and chips = 250 kcal
  - margaritas corrected after Bobby sent `5.5`: 5.5 margaritas = 1100 kcal, C 143g
- Final 2026-06-01 nutrition total: 2880 kcal, C 272g, F 101g, P 70g.

## Watch inventory app — retail vs paid split
- Watch app paths:
  - live source: `/Users/bobby/.openclaw/workspace/watch_inventory_app`
  - public mirror: `/Users/bobby/.openclaw/workspace/watch_inventory_public`
  - live LaunchAgent: `ai.openclaw.watch-inventory-app`, port `4313`
- User goal: paid/cash-basis and retail/reference/display value must be separate. Retail totals should not distort paid basis/accounting math.
- Implemented in `watch_inventory_app/server.mjs`:
  - Added `retail_value(watch, fallback)` helper.
  - Summary collection retail totals use explicit `retail_value` when set, otherwise existing `paid_value` / `trade_in_value` fallback.
  - `paid_value` remains actual paid/cash basis.
  - POST `/api/watch` persists optional `retail_value`.
  - Trade-new-watch creation includes `retail_value`.
- Implemented in `watch_inventory_app/web/app.js` and CSS:
  - Added frontend `retailValue(w, fallback)` helper.
  - Inventory editor forms include `Paid value` and optional `Retail value`.
  - Cards show retail/reference value as the primary number when an explicit retail value exists, and show paid cost as a clean secondary line only when both values exist and differ.
  - Corrected earlier bad UI behavior where fallback paid/current values were labeled as “Retail value,” making it look like every card had invented retail.
  - Actual data check found only two explicit retail overrides at that time: Explorer `475`, DIW Tiffany `375`; “all cards show 375” was likely display/cache/fallback-label confusion, not saved data for every watch.
  - PWA/service-worker cache needed version bump/refresh so stale JS/CSS would not keep rendering the old labels.
- Mirrored watch app code changes to `watch_inventory_public`.
- Syntax checks passed:
  - `node --check watch_inventory_app/server.mjs`
  - `node --check watch_inventory_app/web/app.js`
  - public copies too.
- Isolated API test passed:
  - test watch with `paid_value=300`, `retail_value=500`
  - API returned `carried_basis=300`
  - summary retail total used `500`.
- Live service restarted after patches and live API showed 26 watches with `retailOverrides: 2`.

## Watch inventory accounting perspective and user decision
- Bobby asked whether retail-vs-paid delta should count for realized and unrealized gains.
- Guidance given:
  - Realized P/L should stay strict cash math: sale proceeds minus carried/cash basis (including closed trade chains as appropriate).
  - Retail-vs-paid delta should count as unrealized/reference upside only while still holding the watch.
  - Trade value delta should be shown as context, but not double-counted into realized cash P/L until the chain closes via sale.
  - Monthly compensation watches should keep special handling: they are not outside cash paid, but can carry full reference/display value and create realized recycled capital when sold.
- User said “Ok cool, let’s do that.” Implementation intent: add separate clean accounting fields/labels:
  - `realized` = closed sale cash P/L only.
  - `unrealized retail spread` = current explicit/default retail/reference value minus carried basis for on-hand/pending watches.
  - `trade context` = value gained/lost from trades shown separately, not added on top of retail spread if it would double count.
- Important nuance caught during implementation: adding trade delta on top of retail spread double-counts some traded watches because trade-in value already feeds current on-hand reference value. Correct formula for `total_unrealized_retail`: retail/reference value minus carried basis. Show trade delta/context separately.
- Bobby asked what “trade context 875” means and whether `5980` is monthly comp watches counted as full value paid plus retail spread plus trade. Response should explain plainly:
  - `5980` likely represents lifetime/monthly compensation reference value, not cash Bobby paid out of pocket.
  - Retail spread is separate: current reference/retail value minus carried basis.
  - Trade context `875` means estimated value gain from trade deals (incoming trade/reference value minus outgoing trade/reference value or similar), but it is context only unless sold.
  - Avoid implying trade context is cash profit.
